const request = require("supertest");

const TEST_NAME = "encodings";

describe("Ordinary filenames", () => {
  test("simple url", async () => {
    const objectPath = "a.txt";
    expect(true).toBe(true);
  });
});